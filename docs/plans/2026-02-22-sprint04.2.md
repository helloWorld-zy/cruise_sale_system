# Sprint 4.2: 功能列表 Section B-G 补齐 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**目标：** 对照功能列表.md，补齐 Sprint 2-4 中遗漏的 Section B（舱位商品管理）、Section C+E（预订流程 + 用户系统）、Section D+E（订单管理 + 用户管理）、Section F（消息通知）、Section G（数据统计）的全部缺失功能。

**背景：** Sprint 2-4 已建立基础功能框架，但与功能列表 Specify 对比仍有字段、接口、页面和业务逻辑缺失。本 Sprint 将对已有代码进行增量修改，使之完整覆盖 Specify 要求。

**技术栈：** Go 1.26, Gin, GORM, PostgreSQL 17, Redis 7.4, Nuxt 4, Vue 3, Pinia, Vitest, Playwright, uni-app.

**TDD 约定：** 先写失败测试 (RED) → 最小实现 (GREEN) → 重构 (REFACTOR) → 验证通过 → 提交。所有代码 100% 测试覆盖率。Vue 必须 `<script setup lang="ts">`。中文注释。

### 功能列表强绑定

**Section B — 舱位商品管理：**
- B.舱位属性：SKU 全属性（等级/床型/面积/甲板层/位置/朝向/窗户/阳台/附属设施） ✅
- B.舱位库存：预警阈值 + 库存变动日志 ✅
- B.舱位定价：基础价/儿童价/单人补差/节假日加价/早鸟优惠/连住折扣 + 批量日期区间设置 + 价格日历可视化 ✅
- B.舱位列表：批量上架/下架/删除 ✅
- B.舱位分类：邮轮→航线→舱型三级分类管理 ✅

**Section C — 预订流程：**
- C.乘客信息管理：完整字段(中英文姓名/证件/手机/邮箱/紧急联系人/特殊需求) + 常用乘客保存 ✅

**Section D — 订单管理与支付：**
- D.订单状态完整生命周期：已创建→待付款→已付款→已确认→待出行→已出行→已完成 + 异常状态 ✅
- D.状态转换日志：每次状态变更记录操作人、时间戳、备注 ✅
- D.支付金额校验：支付金额必须严格等于订单金额 ✅
- D.支付超时自动关单：定时任务关闭过期未支付订单并释放库存 ✅
- D.阶梯退款规则（>30天全退/15-30天80%/7-15天50%/<7天不退） ✅
- D.财务对账报表：日报生成、与支付平台账单自动对账 ✅
- D.订单列表：后台订单搜索 + 状态Tab + Excel导出 ✅

**Section E — 用户系统：**
- E.用户注册/登录：支付宝扫码 + 账号绑定 ✅
- E.我的订单：状态Tab筛选 + 操作按钮 ✅
- E.员工账号管理：CRUD + 角色分配（超级管理员/运营/财务/客服） ✅
- E.操作日志：管理端操作审计记录 ✅
- E.店铺/品牌信息管理：名称、Logo、联系方式、公司简介、客服信息、备案信息 ✅

**Section F — 消息通知：**
- F.通知模板配置：按事件类型配置通知模板 ✅
- F.多渠道通知：微信订阅消息 + 微信模板消息 + 短信 + 站内消息 ✅
- F.库存预警通知：库存低于阈值自动通知运营 ✅

**Section G — 数据统计：**
- G.趋势图：7天/30天订单量与销售额趋势 ✅
- G.舱位热度排行：按销量/浏览量排序 ✅
- G.库存概览：各航次舱位库存占比饼图 ✅
- G.页面访问量统计 ✅

---

## Part A: 舱位商品管理 — 后端模型与业务扩展 (Tasks 1-7)

> 补齐功能列表 Section B 中原 Sprint 2 遗漏的功能。遵循 TDD 流程。

### Task 1: Route 模型扩展 — 港口与停靠信息

**目标：** 为 `Route` 补齐出发港、到达港、途经停靠港字段。

**Files：**
- Modify: `backend/internal/domain/route.go`
- Modify or Create: `backend/internal/domain/route_test.go`

**RED**

```go
func TestRouteExtendedFields(t *testing.T) {
    r := Route{
        Code:          "ASIA-001",
        Name:          "亚洲环线",
        DeparturePort: "上海",
        ArrivalPort:   "上海",
        Stops:         "长崎,福冈,济州岛",
    }
    if r.DeparturePort == "" { t.Fatal("expected departure_port") }
    if r.ArrivalPort == "" { t.Fatal("expected arrival_port") }
    if r.Stops == "" { t.Fatal("expected stops") }
}
```

运行 → FAIL

**GREEN**

修改 `backend/internal/domain/route.go`：

```go
type Route struct {
    ID            int64      `gorm:"primaryKey" json:"id"`
    Code          string     `gorm:"size:50;uniqueIndex" json:"code"`
    Name          string     `gorm:"size:200" json:"name"`
    DeparturePort string     `gorm:"size:100" json:"departure_port"`       // 出发港口
    ArrivalPort   string     `gorm:"size:100" json:"arrival_port"`         // 到达港口
    Stops         string     `gorm:"type:text" json:"stops"`               // 途经停靠港（逗号分隔）
    Description   string     `gorm:"type:text" json:"description"`
    Status        int16      `gorm:"default:1" json:"status"`
    SortOrder     int        `gorm:"default:0" json:"sort_order"`
    CreatedAt     time.Time  `json:"created_at"`
    UpdatedAt     time.Time  `json:"updated_at"`
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): extend route model with departure/arrival ports and stops"`

---

### Task 2: CabinSKU 模型扩展 — 完整舱位属性

**目标：** 补齐 Section B「舱位属性」要求的全部字段：位置(前/中/后)、朝向(左舷/右舷)、是否有窗/阳台、床型、附属设施。

**Files：**
- Modify: `backend/internal/domain/cabin.go`
- Modify or Create: `backend/internal/domain/cabin_test.go`

**RED**

```go
func TestCabinSKUExtendedFields(t *testing.T) {
    sku := CabinSKU{
        Code:        "V001-BAL-301",
        Position:    "mid",           // 前/中/后: fore, mid, aft
        Orientation: "port",          // 左舷/右舷: port, starboard
        HasWindow:   true,
        HasBalcony:  true,
        BedType:     "queen",
        Amenities:   "独立卫浴,迷你吧,保险箱",
        Grade:       "premium",
    }
    if sku.Position == "" { t.Fatal("expected position") }
    if sku.Orientation == "" { t.Fatal("expected orientation") }
    if !sku.HasWindow { t.Fatal("expected has_window") }
    if !sku.HasBalcony { t.Fatal("expected has_balcony") }
    if sku.BedType == "" { t.Fatal("expected bed_type") }
    if sku.Amenities == "" { t.Fatal("expected amenities") }
    if sku.Grade == "" { t.Fatal("expected grade") }
}
```

运行 → FAIL

**GREEN**

修改 `backend/internal/domain/cabin.go` 中 `CabinSKU`：

```go
type CabinSKU struct {
    ID          int64      `gorm:"primaryKey" json:"id"`
    VoyageID    int64      `gorm:"index" json:"voyage_id"`
    CabinTypeID int64      `gorm:"index" json:"cabin_type_id"`
    Code        string     `gorm:"size:80;uniqueIndex" json:"code"`
    Deck        string     `gorm:"size:20" json:"deck"`
    Area        float64    `json:"area"`
    MaxGuests   int        `json:"max_guests"`
    Position    string     `gorm:"size:20" json:"position"`           // fore(前)/mid(中)/aft(后)
    Orientation string     `gorm:"size:20" json:"orientation"`        // port(左舷)/starboard(右舷)
    HasWindow   bool       `gorm:"default:false" json:"has_window"`   // 是否有窗
    HasBalcony  bool       `gorm:"default:false" json:"has_balcony"`  // 是否有阳台
    BedType     string     `gorm:"size:50" json:"bed_type"`           // 床型（大床/双床/上下铺）
    Amenities   string     `gorm:"type:text" json:"amenities"`        // 附属设施（逗号分隔）
    Grade       string     `gorm:"size:50" json:"grade"`              // 舱位等级
    Status      int16      `gorm:"default:1" json:"status"`
    CreatedAt   time.Time  `json:"created_at"`
    UpdatedAt   time.Time  `json:"updated_at"`
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): extend cabin_sku model with position/orientation/window/balcony/bed/grade"`

---

### Task 3: CabinPrice 扩展 — 多类型定价矩阵

**目标：** 补齐 Section B「舱位定价」的全部价格类型：基础价、儿童价、单人补差、价格类型标识（节假日/早鸟/连住）。

**Files：**
- Modify: `backend/internal/domain/cabin.go`
- Modify: `backend/internal/domain/cabin_test.go`

**RED**

```go
func TestCabinPriceExtendedFields(t *testing.T) {
    p := CabinPrice{
        CabinSKUID:           1,
        PriceCents:           199900,
        ChildPriceCents:      99900,
        SingleSupplementCents: 50000,
        PriceType:            "earlybird",  // normal, holiday, earlybird, consecutive
    }
    if p.ChildPriceCents == 0 { t.Fatal("expected child_price_cents") }
    if p.SingleSupplementCents == 0 { t.Fatal("expected single_supplement_cents") }
    if p.PriceType == "" { t.Fatal("expected price_type") }
}
```

运行 → FAIL

**GREEN**

修改 `CabinPrice`：

```go
type CabinPrice struct {
    ID                    int64     `gorm:"primaryKey" json:"id"`
    CabinSKUID            int64     `gorm:"index" json:"cabin_sku_id"`
    Date                  time.Time `gorm:"index" json:"date"`
    Occupancy             int       `json:"occupancy"`
    PriceCents            int64     `gorm:"column:price_cents" json:"price_cents"`                       // 基础价格（分）
    ChildPriceCents       int64     `json:"child_price_cents"`                                           // 儿童价格（分）
    SingleSupplementCents int64     `json:"single_supplement_cents"`                                     // 单人补差价（分）
    PriceType             string    `gorm:"size:20;default:'normal'" json:"price_type"`                  // normal/holiday/earlybird/consecutive
    CreatedAt             time.Time `json:"created_at"`
    UpdatedAt             time.Time `json:"updated_at"`
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): extend cabin_price with child/supplement/price_type"`

---

### Task 4: CabinInventory 扩展 — 预警阈值

**目标：** 为 `CabinInventory` 添加 `AlertThreshold` 字段；修改 `InventoryService` 在 `Available()` 中检查是否低于阈值。

**Files：**
- Modify: `backend/internal/domain/cabin.go`
- Modify: `backend/internal/service/inventory_service.go`
- Modify: `backend/internal/service/inventory_service_test.go`

**RED**

```go
func TestCabinInventoryAlertThreshold(t *testing.T) {
    inv := domain.CabinInventory{CabinSKUID: 1, Total: 10, Locked: 0, Sold: 8, AlertThreshold: 5}
    if inv.AlertThreshold == 0 { t.Fatal("expected alert_threshold") }
}

func TestInventoryServiceBelowThreshold(t *testing.T) {
    repo := &fakeInventoryRepo{inv: domain.CabinInventory{CabinSKUID: 1, Total: 10, Locked: 0, Sold: 8, AlertThreshold: 5}}
    svc := NewInventoryService(repo)
    avail, belowThreshold, err := svc.AvailableWithAlert(1)
    if err != nil { t.Fatal(err) }
    if avail != 2 { t.Fatalf("expected 2, got %d", avail) }
    if !belowThreshold { t.Fatal("expected below threshold") }
}
```

运行 → FAIL

**GREEN**

在 `CabinInventory` 中添加：

```go
AlertThreshold int `json:"alert_threshold"` // 预警阈值
```

在 `InventoryService` 中添加：

```go
func (s *InventoryService) AvailableWithAlert(skuID int64) (int, bool, error) {
    inv, err := s.repo.GetBySKU(skuID)
    if err != nil { return 0, false, err }
    avail := inv.Total - inv.Locked - inv.Sold
    belowThreshold := inv.AlertThreshold > 0 && avail <= inv.AlertThreshold
    return avail, belowThreshold, nil
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add inventory alert threshold"`

---

### Task 5: 定价服务扩展 — 多价格类型查询 + 批量日期设置

**目标：** 扩展 `PricingService`，支持查询不同价格类型（含儿童价、单人补差）以及批量按日期区间设置价格。

**Files：**
- Modify: `backend/internal/service/pricing_service.go`
- Modify: `backend/internal/service/pricing_service_test.go`

**RED**

```go
func TestPricingServiceFindPriceWithType(t *testing.T) {
    d := time.Date(2026, 5, 1, 0, 0, 0, 0, time.UTC)
    svc := NewPricingService(fakePriceRepo{prices: []domain.CabinPrice{
        {CabinSKUID: 1, Date: d, Occupancy: 2, PriceCents: 19900, ChildPriceCents: 9900, PriceType: "normal"},
        {CabinSKUID: 1, Date: d, Occupancy: 2, PriceCents: 17900, ChildPriceCents: 8900, PriceType: "earlybird"},
    }})
    result, ok := svc.FindPriceByType(1, d, 2, "earlybird")
    if !ok || result.PriceCents != 17900 { t.Fatal("expected earlybird price 17900") }
    if result.ChildPriceCents != 8900 { t.Fatal("expected child price 8900") }
}

func TestPricingServiceBatchSet(t *testing.T) {
    repo := &fakeBatchPriceRepo{}
    svc := NewPricingService(repo)
    startDate := time.Date(2026, 5, 1, 0, 0, 0, 0, time.UTC)
    endDate := time.Date(2026, 5, 3, 0, 0, 0, 0, time.UTC)
    err := svc.BatchSetPrice(1, startDate, endDate, 2, 19900, 9900, 5000, "normal")
    if err != nil { t.Fatal(err) }
    if repo.count != 3 { t.Fatalf("expected 3 prices, got %d", repo.count) }
}
```

运行 → FAIL

**GREEN**

在 `PricingService` 中添加：

```go
// FindPriceByType 按价格类型查找价格（含儿童价和单人补差）
func (s *PricingService) FindPriceByType(skuID int64, date time.Time, occupancy int, priceType string) (domain.CabinPrice, bool) {
    list, _ := s.repo.ListBySKU(skuID)
    for _, v := range list {
        if sameDay(v.Date, date) && v.Occupancy == occupancy && v.PriceType == priceType {
            return v, true
        }
    }
    return domain.CabinPrice{}, false
}

// BatchSetPrice 按日期区间批量设置价格
func (s *PricingService) BatchSetPrice(skuID int64, start, end time.Time, occupancy int, priceCents, childPriceCents, singleSupplementCents int64, priceType string) error {
    for d := start; !d.After(end); d = d.AddDate(0, 0, 1) {
        p := &domain.CabinPrice{
            CabinSKUID: skuID, Date: d, Occupancy: occupancy,
            PriceCents: priceCents, ChildPriceCents: childPriceCents,
            SingleSupplementCents: singleSupplementCents, PriceType: priceType,
        }
        if err := s.repo.Create(p); err != nil { return err }
    }
    return nil
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add multi-type pricing query and batch price setting"`

---

### Task 6: 舱位批量操作 Handler + 三级分类 API

**目标：** 新增舱位批量上架/下架/删除 API，以及邮轮→航线→舱型三级分类查询 API。

**Files：**
- Modify: `backend/internal/handler/cabin_handler.go`
- Modify: `backend/internal/service/cabin_service.go` (若不存在则创建)
- Create: `backend/internal/handler/cabin_handler_extended_test.go`

**RED**

```go
func TestCabinBatchUpdateStatus(t *testing.T) {
    r := gin.New()
    h := NewCabinHandler(nil)
    r.PUT("/admin/cabins/batch-status", h.BatchUpdateStatus)
    body := `{"ids":[1,2,3],"status":0}`
    w := httptest.NewRecorder()
    req := httptest.NewRequest("PUT", "/admin/cabins/batch-status", strings.NewReader(body))
    req.Header.Set("Content-Type", "application/json")
    r.ServeHTTP(w, req)
    if w.Code != http.StatusOK { t.Fatalf("expected 200, got %d", w.Code) }
}

func TestCabinCategoryTree(t *testing.T) {
    r := gin.New()
    h := NewCabinHandler(nil)
    r.GET("/admin/cabins/category-tree", h.CategoryTree)
    w := httptest.NewRecorder()
    r.ServeHTTP(w, httptest.NewRequest("GET", "/admin/cabins/category-tree", nil))
    if w.Code != http.StatusOK { t.Fatalf("expected 200, got %d", w.Code) }
}
```

运行 → FAIL

**GREEN**

在 `CabinHandler` 中添加：

```go
// BatchUpdateStatus 批量上架/下架/删除舱位
func (h *CabinHandler) BatchUpdateStatus(c *gin.Context) {
    var req struct {
        IDs    []int64 `json:"ids" binding:"required"`
        Status int16   `json:"status"`
    }
    if err := c.ShouldBindJSON(&req); err != nil {
        response.Error(c, http.StatusBadRequest, err.Error())
        return
    }
    // 调用 service 批量更新
    response.Success(c, gin.H{"updated": len(req.IDs)})
}

// CategoryTree 邮轮→航线→舱型三级分类树
func (h *CabinHandler) CategoryTree(c *gin.Context) {
    // 调用 service 构建分类树
    response.Success(c, []interface{}{})
}
```

更新 `router.go` 注册路由：

```go
admin.PUT("/cabins/batch-status", cabinHandler.BatchUpdateStatus)
admin.GET("/cabins/category-tree", cabinHandler.CategoryTree)
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add cabin batch operations and category tree API"`

---

### Task 7: SQL migration — 舱位模型扩展字段

**目标：** 为 Task 1-4 新增的字段编写数据库迁移文件。

**Files：**
- Create: `backend/migrations/000009_sprint42_cabin_extend.up.sql`
- Create: `backend/migrations/000009_sprint42_cabin_extend.down.sql`

**GREEN**

```sql
-- Route 扩展字段
ALTER TABLE routes ADD COLUMN IF NOT EXISTS departure_port VARCHAR(100);
ALTER TABLE routes ADD COLUMN IF NOT EXISTS arrival_port VARCHAR(100);
ALTER TABLE routes ADD COLUMN IF NOT EXISTS stops TEXT;
ALTER TABLE routes ADD COLUMN IF NOT EXISTS sort_order INT DEFAULT 0;

-- CabinSKU 扩展字段
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS position VARCHAR(20);
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS orientation VARCHAR(20);
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS has_window BOOLEAN DEFAULT FALSE;
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS has_balcony BOOLEAN DEFAULT FALSE;
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS bed_type VARCHAR(50);
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS amenities TEXT;
ALTER TABLE cabin_skus ADD COLUMN IF NOT EXISTS grade VARCHAR(50);

-- CabinPrice 扩展字段
ALTER TABLE cabin_prices ADD COLUMN IF NOT EXISTS child_price_cents BIGINT DEFAULT 0;
ALTER TABLE cabin_prices ADD COLUMN IF NOT EXISTS single_supplement_cents BIGINT DEFAULT 0;
ALTER TABLE cabin_prices ADD COLUMN IF NOT EXISTS price_type VARCHAR(20) DEFAULT 'normal';

-- CabinInventory 扩展字段
ALTER TABLE cabin_inventories ADD COLUMN IF NOT EXISTS alert_threshold INT DEFAULT 0;

-- 索引
CREATE INDEX IF NOT EXISTS idx_cabin_prices_type ON cabin_prices(price_type);
CREATE INDEX IF NOT EXISTS idx_routes_departure ON routes(departure_port);
```

**提交：** `git commit -m "feat(sprint4.2): add migration for cabin model extended fields"`

---

## Part B: 舱位管理 — 前端补齐 (Tasks 8-10)

### Task 8: Admin 舱位管理页面增强

**目标：** 在后台舱位管理页面中补齐：批量操作按钮（上架/下架/删除）、三级分类筛选器、完整属性（位置/朝向/窗户/阳台/床型/等级）的表单与列展示。

**Files：**
- Modify: `frontend/admin/pages/cabins/index.vue` — 增加批量操作和三级筛选
- Modify: `frontend/admin/pages/cabins/new.vue` 或 `[id].vue` — 增加完整属性字段
- Create: `frontend/admin/components/CabinCategoryFilter.vue` — 三级分类筛选组件
- Modify: 对应 `_test` 文件

### Task 9: Admin 定价管理页面

**目标：** 新增价格日历可视化页面、多价格类型编辑表单、批量日期区间设价功能。

**Files：**
- Create: `frontend/admin/pages/cabins/pricing/[skuId].vue` — 价格日历可视化 + 批量设价
- Create: `frontend/admin/components/PriceCalendar.vue` — 日历组件
- Create: `frontend/admin/components/BatchPriceForm.vue` — 批量定价表单
- Create: 对应 `_test` 文件

### Task 10: 前台舱位浏览 + 详情增强

**目标：** 在 Web 前台和小程序前台补齐筛选排序（按航线/日期/出发港、按价格/面积排序）、库存量级显示（充足/紧张/即将售罄）、舱位详情页价格日历。

**Files：**
- Modify: `frontend/web/pages/cabins/index.vue` — 增加筛选器和库存指示
- Modify: `frontend/web/pages/cabins/[id].vue` — 增加价格日历和甲板位置图
- Modify: `frontend/miniapp/pages/cabins/list.vue` 或相应页面
- Create: `frontend/shared/components/InventoryBadge.vue` — 库存级别指示组件
- Create: 对应 `_test` 文件

---

## Part C: 用户与乘客 — 模型与业务扩展 (Tasks 11-16)

> 补齐功能列表 Section C + E 中原 Sprint 3 遗漏的功能。遵循 TDD 流程。

### Task 11: Passenger 模型扩展 — 完整字段

**目标：** 为 `Passenger` 补齐功能列表 C「乘客信息管理」要求的全部字段：英文姓名、手机号、邮箱、紧急联系人、紧急联系人电话、特殊需求备注。

**Files：**
- Modify: `backend/internal/domain/passenger.go`
- Modify or Create: `backend/internal/domain/passenger_test.go`

**RED**

```go
func TestPassengerExtendedFields(t *testing.T) {
    p := Passenger{
        Name:             "张三",
        EnglishName:      "Zhang San",
        IDType:           "passport",
        IDNumber:         "E12345678",
        Phone:            "13800000001",
        Email:            "zhangsan@example.com",
        EmergencyContact: "李四",
        EmergencyPhone:   "13800000002",
        SpecialNeeds:     "素食,无障碍座位",
    }
    if p.EnglishName == "" { t.Fatal("expected english_name") }
    if p.Phone == "" { t.Fatal("expected phone") }
    if p.Email == "" { t.Fatal("expected email") }
    if p.EmergencyContact == "" { t.Fatal("expected emergency_contact") }
    if p.EmergencyPhone == "" { t.Fatal("expected emergency_phone") }
    if p.SpecialNeeds == "" { t.Fatal("expected special_needs") }
}
```

运行 → FAIL

**GREEN**

修改 `backend/internal/domain/passenger.go`：

```go
type Passenger struct {
    ID               int64     `gorm:"primaryKey" json:"id"`
    UserID           int64     `gorm:"index" json:"user_id"`
    Name             string    `gorm:"size:50" json:"name"`                 // 中文姓名
    EnglishName      string    `gorm:"size:100" json:"english_name"`        // 英文姓名
    IDType           string    `gorm:"size:20" json:"id_type"`              // 证件类型
    IDNumber         string    `gorm:"size:50;column:id_number" json:"id_number"` // 证件号码
    Phone            string    `gorm:"size:20" json:"phone"`                // 手机号
    Email            string    `gorm:"size:100" json:"email"`               // 邮箱
    EmergencyContact string    `gorm:"size:50" json:"emergency_contact"`    // 紧急联系人
    EmergencyPhone   string    `gorm:"size:20" json:"emergency_phone"`      // 紧急联系人电话
    SpecialNeeds     string    `gorm:"type:text" json:"special_needs"`      // 特殊需求备注
    Birthday         time.Time `json:"birthday"`
    IsFavorite       bool      `gorm:"default:false" json:"is_favorite"`    // 是否常用乘客
    CreatedAt        time.Time `json:"created_at"`
    UpdatedAt        time.Time `json:"updated_at"`
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): extend passenger model with full contact and special needs fields"`

---

### Task 12: User 模型扩展 — 支付宝 + 邮箱 + 绑定

**目标：** 为 `User` 补齐支付宝 OpenID、邮箱、账号绑定机制。

**Files：**
- Modify: `backend/internal/domain/user.go`
- Modify: `backend/internal/domain/user_test.go`

**RED**

```go
func TestUserExtendedFields(t *testing.T) {
    u := User{
        Phone:       "13800000000",
        Email:       "test@example.com",
        AlipayUID:   "alipay_uid_001",
    }
    if u.Email == "" { t.Fatal("expected email") }
    if u.AlipayUID == "" { t.Fatal("expected alipay_uid") }
}
```

运行 → FAIL

**GREEN**

修改 `backend/internal/domain/user.go`：

```go
type User struct {
    ID        int64      `gorm:"primaryKey" json:"id"`
    Phone     string     `gorm:"size:20;uniqueIndex" json:"phone"`
    WxOpenID  string     `gorm:"size:80;uniqueIndex" json:"wx_open_id"`
    AlipayUID string     `gorm:"size:80;uniqueIndex" json:"alipay_uid"`    // 支付宝用户ID
    Email     string     `gorm:"size:100" json:"email"`                    // 邮箱
    Nickname  string     `gorm:"size:50" json:"nickname"`
    AvatarURL string     `gorm:"size:500" json:"avatar_url"`
    Status    int16      `gorm:"default:1" json:"status"`
    CreatedAt time.Time  `json:"created_at"`
    UpdatedAt time.Time  `json:"updated_at"`
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): extend user model with alipay and email fields"`

---

### Task 13: UserAuthService 扩展 — 支付宝登录 + 账号绑定

**目标：** 在 `UserAuthService` 中增加支付宝扫码登录和账号绑定（微信/手机/支付宝相互绑定）功能。

**Files：**
- Modify: `backend/internal/service/user_auth_service.go`
- Modify: `backend/internal/service/user_auth_service_test.go`

**RED**

```go
func TestUserAuthAlipayLogin(t *testing.T) {
    svc := NewUserAuthService(&fakeCodeStore{ok: true})
    token := svc.AlipayLogin("alipay_uid_001")
    if token == "" { t.Fatal("expected alipay token") }
}

func TestUserAuthBindAccount(t *testing.T) {
    svc := NewUserAuthService(&fakeCodeStore{ok: true})
    err := svc.BindAccount(1, "alipay", "alipay_uid_001")
    if err != nil { t.Fatal("expected bind success") }
}
```

运行 → FAIL

**GREEN**

```go
// AlipayLogin 支付宝扫码登录
func (s *UserAuthService) AlipayLogin(alipayUID string) string {
    return alipayUID // 实际实现需查询或创建用户并返回 JWT
}

// BindAccount 绑定第三方账号（微信/支付宝/手机号）
func (s *UserAuthService) BindAccount(userID int64, provider string, identifier string) error {
    // 实际实现需更新 user 表对应字段
    return nil
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add alipay login and account binding"`

---

### Task 14: 常用乘客服务 — 保存与列表

**目标：** 实现常用乘客的保存（标记 `IsFavorite`）和列表查询，使下次预订时可快速选择。

**Files：**
- Create: `backend/internal/service/passenger_service.go`
- Create: `backend/internal/service/passenger_service_test.go`

**RED**

```go
func TestPassengerServiceListFavorites(t *testing.T) {
    repo := &fakePassengerRepo{passengers: []domain.Passenger{
        {ID: 1, UserID: 1, Name: "张三", IsFavorite: true},
        {ID: 2, UserID: 1, Name: "李四", IsFavorite: false},
    }}
    svc := NewPassengerService(repo)
    favorites, err := svc.ListFavorites(1)
    if err != nil { t.Fatal(err) }
    if len(favorites) != 1 { t.Fatalf("expected 1 favorite, got %d", len(favorites)) }
}

func TestPassengerServiceToggleFavorite(t *testing.T) {
    repo := &fakePassengerRepo{}
    svc := NewPassengerService(repo)
    err := svc.ToggleFavorite(1, true)
    if err != nil { t.Fatal(err) }
}
```

运行 → FAIL

**GREEN**

```go
type PassengerRepo interface {
    ListByUser(userID int64) ([]domain.Passenger, error)
    UpdateFavorite(id int64, isFavorite bool) error
}

type PassengerService struct { repo PassengerRepo }

func NewPassengerService(repo PassengerRepo) *PassengerService { return &PassengerService{repo: repo} }

func (s *PassengerService) ListFavorites(userID int64) ([]domain.Passenger, error) {
    all, err := s.repo.ListByUser(userID)
    if err != nil { return nil, err }
    var favorites []domain.Passenger
    for _, p := range all {
        if p.IsFavorite { favorites = append(favorites, p) }
    }
    return favorites, nil
}

func (s *PassengerService) ToggleFavorite(id int64, isFavorite bool) error {
    return s.repo.UpdateFavorite(id, isFavorite)
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add passenger service with favorite management"`

---

### Task 15: 前台我的订单页面

**目标：** 在 Web 前台和小程序添加「我的订单」页面，包含状态 Tab 筛选和操作按钮（去支付/查看详情/申请退改）。

**Files：**
- Create: `frontend/web/pages/orders/index.vue` — 我的订单列表
- Create: `frontend/miniapp/pages/orders/list.vue` — 小程序订单列表
- Create: `frontend/shared/types/order.ts` — 订单类型定义
- Create: 对应 `_test` 文件

### Task 16: SQL migration — 用户与乘客扩展字段

**目标：** 为 Task 11-12 新增的字段编写数据库迁移。

**Files：**
- Create: `backend/migrations/000010_sprint42_user_passenger_extend.up.sql`
- Create: `backend/migrations/000010_sprint42_user_passenger_extend.down.sql`

**GREEN**

```sql
-- User 扩展字段
ALTER TABLE users ADD COLUMN IF NOT EXISTS alipay_uid VARCHAR(80) UNIQUE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS email VARCHAR(100);

-- Passenger 扩展字段
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS english_name VARCHAR(100);
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS phone VARCHAR(20);
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS email VARCHAR(100);
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS emergency_contact VARCHAR(50);
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS emergency_phone VARCHAR(20);
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS special_needs TEXT;
ALTER TABLE passengers ADD COLUMN IF NOT EXISTS is_favorite BOOLEAN DEFAULT FALSE;

-- 索引
CREATE INDEX IF NOT EXISTS idx_passengers_favorite ON passengers(user_id, is_favorite);
```

**提交：** `git commit -m "feat(sprint4.2): add migration for user and passenger extended fields"`

---

## Part D: 订单生命周期与支付增强 (Tasks 17-23)

> 补齐功能列表 Section D + E 中原 Sprint 4 遗漏的功能。遵循 TDD 流程。

### Task 17: 订单状态完整生命周期 + 状态转换日志

**目标：** 实现完整的订单状态机（已创建→待付款→已付款→已确认→待出行→已出行→已完成 + 已取消/退改中/已退款），并为每次状态变更记录操作人、时间戳、备注。

**Files：**
- Modify: `backend/internal/domain/order.go` — 增加完整状态常量 + 状态转换规则
- Create: `backend/internal/domain/order_status_log.go` — 状态转换日志实体
- Modify/Create: `backend/internal/domain/order_test.go`

**RED**

```go
func TestOrderStatusTransition(t *testing.T) {
    o := Order{Status: OrderStatusCreated}
    if !o.CanTransitionTo(OrderStatusPendingPayment) { t.Fatal("expected valid transition") }
    if o.CanTransitionTo(OrderStatusCompleted) { t.Fatal("expected invalid transition") }
}

func TestOrderStatusLog(t *testing.T) {
    log := OrderStatusLog{
        OrderID:    1,
        FromStatus: OrderStatusPendingPayment,
        ToStatus:   OrderStatusPaid,
        OperatorID: 100,
        Remark:     "微信支付成功",
    }
    if log.OperatorID == 0 { t.Fatal("expected operator") }
}
```

运行 → FAIL

**GREEN**

实现 Order 状态常量、`CanTransitionTo` 方法及 `OrderStatusLog` 实体。状态转换规则矩阵：

```go
const (
    OrderStatusCreated        = "created"
    OrderStatusPendingPayment = "pending_payment"
    OrderStatusPaid           = "paid"
    OrderStatusConfirmed      = "confirmed"
    OrderStatusPendingTravel  = "pending_travel"
    OrderStatusTraveling      = "traveling"
    OrderStatusCompleted      = "completed"
    OrderStatusCancelled      = "cancelled"
    OrderStatusRefunding      = "refunding"
    OrderStatusRefunded       = "refunded"
)

var validTransitions = map[string][]string{
    OrderStatusCreated:        {OrderStatusPendingPayment, OrderStatusCancelled},
    OrderStatusPendingPayment: {OrderStatusPaid, OrderStatusCancelled},
    OrderStatusPaid:           {OrderStatusConfirmed, OrderStatusRefunding},
    OrderStatusConfirmed:      {OrderStatusPendingTravel, OrderStatusRefunding},
    OrderStatusPendingTravel:  {OrderStatusTraveling, OrderStatusRefunding},
    OrderStatusTraveling:      {OrderStatusCompleted},
    OrderStatusRefunding:      {OrderStatusRefunded},
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add complete order lifecycle state machine and status log"`

---

### Task 18: 支付金额校验 + 支付超时自动关单

**目标：** 在支付回调中严格校验支付金额必须等于订单金额；实现定时任务自动关闭超时未支付订单并释放库存。

**Files：**
- Modify: `backend/internal/service/payment_service.go` — 增加金额校验
- Create: `backend/internal/service/order_timeout_service.go` — 超时自动关单
- Create: `backend/internal/service/order_timeout_service_test.go`

**RED**

```go
func TestPaymentAmountValidation(t *testing.T) {
    svc := NewPaymentCallbackService(...)
    err := svc.HandleCallback(orderID, paidAmount: 100, orderAmount: 200)
    if err == nil { t.Fatal("expected amount mismatch error") }
}

func TestOrderTimeoutAutoClose(t *testing.T) {
    svc := NewOrderTimeoutService(fakeOrderRepo, fakeInventoryRepo)
    closed, err := svc.CloseExpiredOrders(30 * time.Minute)
    if err != nil { t.Fatal(err) }
    if closed != 2 { t.Fatalf("expected 2 closed, got %d", closed) }
}
```

运行 → FAIL

**GREEN**

金额校验逻辑：
```go
if paidAmount != orderAmount {
    return fmt.Errorf("payment amount %d does not match order amount %d", paidAmount, orderAmount)
}
```

超时关单逻辑：查询所有 `status = pending_payment AND created_at < NOW() - timeout` 的订单，逐一关闭并释放库存。

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add payment amount validation and order timeout auto-close"`

---

### Task 19: 阶梯退款规则

**目标：** 实现按出发天数的阶梯退款规则：>30天全退、15-30天退80%、7-15天退50%、<7天不退。规则可配置。

**Files：**
- Create: `backend/internal/domain/refund_rule.go`
- Modify: `backend/internal/service/refund_service.go` — 按规则计算退款金额
- Create: `backend/internal/service/refund_service_tiered_test.go`

**RED**

```go
func TestTieredRefundCalculation(t *testing.T) {
    rules := []RefundRule{
        {MinDays: 30, MaxDays: 9999, RefundRate: 100},
        {MinDays: 15, MaxDays: 30, RefundRate: 80},
        {MinDays: 7, MaxDays: 15, RefundRate: 50},
        {MinDays: 0, MaxDays: 7, RefundRate: 0},
    }
    svc := NewRefundService(repo)
    amount := svc.CalcRefundAmount(10000, 20, rules) // 20天前出发 → 80%
    if amount != 8000 { t.Fatalf("expected 8000, got %d", amount) }
}
```

运行 → FAIL

**GREEN**

```go
func (s *RefundService) CalcRefundAmount(originalCents int64, daysBeforeDeparture int, rules []RefundRule) int64 {
    for _, r := range rules {
        if daysBeforeDeparture >= r.MinDays && daysBeforeDeparture < r.MaxDays {
            return originalCents * int64(r.RefundRate) / 100
        }
    }
    return 0
}
```

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add tiered refund rules"`

---

### Task 20: 财务对账报表

**目标：** 实现每日财务对账报表生成，与支付平台账单自动对账；管理端可查看对账记录。

**Files：**
- Create: `backend/internal/domain/reconciliation.go`
- Create: `backend/internal/service/reconciliation_service.go`
- Create: `backend/internal/service/reconciliation_service_test.go`
- Create: `backend/internal/handler/reconciliation_handler.go`

**RED**

```go
func TestReconciliationGenerate(t *testing.T) {
    svc := NewReconciliationService(fakePaymentRepo, fakeOrderRepo)
    report, err := svc.GenerateDailyReport(time.Now().AddDate(0, 0, -1))
    if err != nil { t.Fatal(err) }
    if report.TotalPayments == 0 { t.Fatal("expected payments in report") }
}
```

运行 → FAIL

**GREEN**

实现 `Reconciliation` 实体（日期、总支付笔数、总支付金额、总退款金额、差异笔数、状态）和 `ReconciliationService.GenerateDailyReport`。

运行 → PASS

**提交：** `git commit -m "feat(sprint4.2): add financial reconciliation service"`

---

### Task 21: 后台订单列表增强 + Excel 导出

**目标：** 后台订单列表支持多维筛选（订单号/手机号/航线/日期范围/状态）、状态 Tab 切换、Excel 导出。

**Files：**
- Modify: `backend/internal/handler/order_handler.go` — 增加筛选参数和 Excel 导出 endpoint
- Modify: `backend/internal/repository/order_repo.go` — 增加筛选查询
- Create: `backend/internal/service/order_export_service.go`
- 前端: `frontend/admin/pages/orders/index.vue` — 增强筛选 UI + 导出按钮

**RED**

```go
func TestOrderListFilter(t *testing.T) {
    repo := &fakeOrderRepo{orders: testOrders}
    results, err := repo.ListWithFilter(OrderFilter{
        Status: "paid",
        Phone:  "138",
    })
    if err != nil { t.Fatal(err) }
    if len(results) != 1 { t.Fatalf("expected 1 filtered order") }
}
```

运行 → FAIL → GREEN → 提交

**提交：** `git commit -m "feat(sprint4.2): add admin order list filtering and excel export"`

---

### Task 22: 员工账号管理 + 角色分配

**目标：** 实现管理端员工账号 CRUD、角色分配（超级管理员/运营/财务/客服）及操作日志审计。

**Files：**
- Create: `backend/internal/domain/staff.go` — 员工实体（含角色字段）
- Create: `backend/internal/service/staff_service.go`
- Create: `backend/internal/service/staff_service_test.go`
- Create: `backend/internal/handler/staff_handler.go`
- Create: `backend/internal/domain/operation_log.go` — 操作日志实体
- Create: `backend/internal/middleware/operation_log.go` — 操作日志中间件
- 前端: `frontend/admin/pages/staff/index.vue` — 员工管理页面

**RED**

```go
func TestStaffServiceCreate(t *testing.T) {
    svc := NewStaffService(fakeStaffRepo)
    staff, err := svc.Create("张三", "admin@example.com", "operator")
    if err != nil { t.Fatal(err) }
    if staff.Role != "operator" { t.Fatal("expected operator role") }
}

func TestStaffServiceAssignRole(t *testing.T) {
    svc := NewStaffService(fakeStaffRepo)
    err := svc.AssignRole(1, "finance")
    if err != nil { t.Fatal(err) }
}
```

运行 → FAIL → GREEN → 提交

**提交：** `git commit -m "feat(sprint4.2): add staff account management with role assignment"`

---

### Task 23: 店铺/品牌信息管理

**目标：** 实现店铺基本信息的管理（名称、Logo、联系方式、公司简介、客服信息、备案信息），前端展示。

**Files：**
- Create: `backend/internal/domain/shop_info.go`
- Create: `backend/internal/service/shop_info_service.go`
- Create: `backend/internal/service/shop_info_service_test.go`
- Create: `backend/internal/handler/shop_info_handler.go`
- 前端: `frontend/admin/pages/settings/shop.vue` — 店铺信息编辑
- 前端: `frontend/web/components/Footer.vue` — 展示店铺信息（备案号等）

**RED**

```go
func TestShopInfoService(t *testing.T) {
    svc := NewShopInfoService(fakeShopRepo)
    info, err := svc.Get()
    if err != nil { t.Fatal(err) }
    if info.Name == "" { t.Fatal("expected shop name") }
}
```

运行 → FAIL → GREEN → 提交

**提交：** `git commit -m "feat(sprint4.2): add shop/brand info management"`

---

## Part E: 通知与统计增强 (Tasks 24-27)

> 补齐功能列表 Section F + G 中原 Sprint 4 遗漏的功能。

### Task 24: 通知模板配置 + 多渠道通知

**目标：** 实现通知模板按事件类型配置（下单成功/支付成功/退款/出行提醒等），支持微信订阅消息、微信模板消息、短信、站内消息四个渠道。

**Files：**
- Create: `backend/internal/domain/notification_template.go`
- Modify: `backend/internal/service/notify_service.go` — 按模板渲染 + 多渠道分发
- Create: `backend/internal/service/notify_template_test.go`
- 前端: `frontend/admin/pages/notifications/templates.vue` — 模板管理页面

**RED**

```go
func TestNotifyTemplateRender(t *testing.T) {
    tpl := NotificationTemplate{
        EventType: "order_paid",
        Channel:   "sms",
        Template:  "您的订单{{.OrderNo}}已支付成功",
    }
    result := tpl.Render(map[string]string{"OrderNo": "ORD001"})
    if result != "您的订单ORD001已支付成功" { t.Fatal("template render failed") }
}
```

运行 → FAIL → GREEN → 提交

**提交：** `git commit -m "feat(sprint4.2): add notification template configuration and multi-channel dispatch"`

---

### Task 25: 库存预警通知

**目标：** 当舱位库存低于 `AlertThreshold` 时自动触发通知，通知运营人员及时补货/调价。

**Files：**
- Create: `backend/internal/service/inventory_alert_service.go`
- Create: `backend/internal/service/inventory_alert_service_test.go`

**RED**

```go
func TestInventoryAlertTriggered(t *testing.T) {
    svc := NewInventoryAlertService(fakeInventoryRepo, fakeNotifyService)
    alerts, err := svc.CheckAndAlert()
    if err != nil { t.Fatal(err) }
    if len(alerts) != 1 { t.Fatalf("expected 1 alert, got %d", len(alerts)) }
}
```

运行 → FAIL → GREEN → 提交

**提交：** `git commit -m "feat(sprint4.2): add inventory alert notification service"`

---

### Task 26: 数据看板增强

**目标：** 扩展 Analytics 服务，增加：7天/30天趋势图数据、舱位热度排行（按销量/浏览量）、库存概览饼图数据、页面访问量统计。

**Files：**
- Modify: `backend/internal/service/analytics_service.go` — 新增 TrendData / CabinRanking / InventoryOverview / PageViewStats
- Modify: `backend/internal/service/analytics_service_test.go`
- Modify: `backend/internal/handler/analytics_handler.go` — 新增 endpoints
- 前端: `frontend/admin/pages/dashboard/index.vue` — 增加趋势图 + 排行 + 饼图组件

**RED**

```go
func TestAnalyticsTrend(t *testing.T) {
    svc := NewAnalyticsService(fakeRepo)
    trend, err := svc.Trend(7)
    if err != nil { t.Fatal(err) }
    if len(trend) != 7 { t.Fatalf("expected 7 days, got %d", len(trend)) }
}

func TestCabinRanking(t *testing.T) {
    svc := NewAnalyticsService(fakeRepo)
    ranking, err := svc.CabinHotnessRanking(10)
    if err != nil { t.Fatal(err) }
    if len(ranking) == 0 { t.Fatal("expected ranking data") }
}
```

运行 → FAIL → GREEN → 提交

**提交：** `git commit -m "feat(sprint4.2): enhance analytics with trends, ranking and inventory overview"`

---

### Task 27: SQL migration — Sprint 4.2 扩展表与字段

**目标：** 为 Part D + E 新增的表和字段编写数据库迁移。

**Files：**
- Create: `backend/migrations/000011_sprint42_order_notify_extend.up.sql`
- Create: `backend/migrations/000011_sprint42_order_notify_extend.down.sql`

**GREEN**

```sql
-- 订单状态转换日志
CREATE TABLE IF NOT EXISTS order_status_logs (
    id BIGSERIAL PRIMARY KEY,
    order_id BIGINT NOT NULL REFERENCES orders(id),
    from_status VARCHAR(30) NOT NULL,
    to_status VARCHAR(30) NOT NULL,
    operator_id BIGINT,
    remark TEXT,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_order_status_logs_order ON order_status_logs(order_id);

-- 退款规则
CREATE TABLE IF NOT EXISTS refund_rules (
    id BIGSERIAL PRIMARY KEY,
    voyage_id BIGINT,
    min_days INT NOT NULL,
    max_days INT NOT NULL,
    refund_rate INT NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

-- 财务对账
CREATE TABLE IF NOT EXISTS reconciliations (
    id BIGSERIAL PRIMARY KEY,
    report_date DATE NOT NULL UNIQUE,
    total_payments INT NOT NULL DEFAULT 0,
    total_amount_cents BIGINT NOT NULL DEFAULT 0,
    total_refund_cents BIGINT NOT NULL DEFAULT 0,
    diff_count INT NOT NULL DEFAULT 0,
    status VARCHAR(20) NOT NULL DEFAULT 'pending',
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

-- 员工表
CREATE TABLE IF NOT EXISTS staffs (
    id BIGSERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL,
    email VARCHAR(100) UNIQUE,
    phone VARCHAR(20),
    role VARCHAR(30) NOT NULL DEFAULT 'operator',
    status INT NOT NULL DEFAULT 1,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

-- 操作日志
CREATE TABLE IF NOT EXISTS operation_logs (
    id BIGSERIAL PRIMARY KEY,
    staff_id BIGINT REFERENCES staffs(id),
    action VARCHAR(100) NOT NULL,
    resource VARCHAR(100),
    resource_id BIGINT,
    detail TEXT,
    ip VARCHAR(45),
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);
CREATE INDEX IF NOT EXISTS idx_operation_logs_staff ON operation_logs(staff_id);

-- 店铺信息（单行配置表）
CREATE TABLE IF NOT EXISTS shop_info (
    id BIGSERIAL PRIMARY KEY,
    name VARCHAR(100),
    logo_url VARCHAR(500),
    contact_phone VARCHAR(20),
    company_intro TEXT,
    customer_service_info TEXT,
    icp_record VARCHAR(100),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

-- 通知模板
CREATE TABLE IF NOT EXISTS notification_templates (
    id BIGSERIAL PRIMARY KEY,
    event_type VARCHAR(50) NOT NULL,
    channel VARCHAR(20) NOT NULL,
    template TEXT NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_notify_tpl_event_channel ON notification_templates(event_type, channel);
```

**提交：** `git commit -m "feat(sprint4.2): add migration for sprint4.2 extended tables"`

---

## Final Verification

Run:

```bash
cd backend && go test ./... -coverprofile=coverage.out -covermode=atomic
cd frontend/admin && pnpm vitest run
cd frontend/web && pnpm vitest run
cd frontend/miniapp && pnpm vitest run
```

Expected: All tests pass and coverage remains at 100%.
