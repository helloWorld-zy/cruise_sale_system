server:
  port: ":8080"
  mode: "debug"
database:
  host: "localhost"
  port: 15432
  user: "cruise"
  # password must be set via CRUISE_DATABASE_PASSWORD env variable
  password: ""
  dbname: "cruise_booking"
  sslmode: "disable"
  maxidleconns: 10
  maxopenconns: 100
  connmaxlifetime: 300
redis:
  host: "localhost"
  port: 6379
  # password must be set via CRUISE_REDIS_PASSWORD env variable
  password: ""
  db: 0
minio:
  endpoint: "localhost:9000"
  accesskey: "minioadmin"
  # secretkey must be set via CRUISE_MINIO_SECRETKEY env variable
  secretkey: ""
  bucket: "cruise-static"
  usessl: false
meilis:
  host: "http://localhost:7700"
  # apikey must be set via CRUISE_MEILIS_APIKEY env variable
  apikey: ""
nats:
  url: "nats://localhost:4222"
jwt:
  # secret must be set via CRUISE_JWT_SECRET env variable
  secret: ""
  expirehours: 24
log:
  level: "debug"
  filename: "logs/app.log"
  maxsize: 100
  maxbackups: 3
  maxage: 7
  compress: true
